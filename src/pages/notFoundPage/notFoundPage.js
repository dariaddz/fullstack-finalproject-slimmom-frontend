import s from './notFoundPage.module.css';

const NotFoundPage = () => {
  return (
    <div className={s.container}>
      <p>на жаль, у нас немає такої сторінки</p>
    </div>
  );
};

export default NotFoundPage;
